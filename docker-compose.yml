version: "3.1"
services:

  web:
    image: node:alpine
    command: npm run dev
    container_name: nodejs
    ports:
      - "3000:3000"
      - "9229:9229"
    volumes:
      - ./todo_api:/application
    working_dir: /application
    restart: "no"

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8091:8081
    environment:
      ME_CONFIG_MONGODB_SERVER: ds229552.mlab.com
      ME_CONFIG_MONGODB_PORT: 29552
      ME_CONFIG_MONGODB_ENABLE_ADMIN: "false"
      ME_CONFIG_MONGODB_AUTH_DATABASE: yury-test
      ME_CONFIG_MONGODB_AUTH_USERNAME: qw4u
      ME_CONFIG_MONGODB_AUTH_PASSWORD: EVSm6pNugKpvEosT
